// src/services/icp.js
/*
import { HttpAgent, Actor } from '@dfinity/agent';
import { idlFactory } from './idlFactory'; // Your candid IDL factory

const canisterId = 'your-canister-id'; // Replace with your canister ID

const agent = new HttpAgent({ host: 'https://ic0.app' });

const icpActor = Actor.createActor(idlFactory, { agent, canisterId });

export default icpActor; 
import { Actor, HttpAgent } from '@dfinity/agent'; 
import { idlFactory } from './Grincoin_backend.did';  // Ensure this path is correct
import { canisterId } from './Grincoin_backend.did';

const agent = new HttpAgent({ host: 'https://mainnet.dfinity.network' });

const icp = Actor.createActor(idlFactory, {
  agent,
  canisterId,
});


export default icp;
*/